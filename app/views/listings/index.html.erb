<% @listings.each do |listing| %>
  <h1><%= listing.name %></h1>
  <p>
    <%= listing.description %>
  </p>
  <%= link_to "View more", listing_path(listing) %>
  <%= link_to "Edit", edit_listing_path(listing) %>
  <hr>
<% end %>
